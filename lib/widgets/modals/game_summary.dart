import 'package:flutter/material.dart';

class GameSummaryModal extends StatelessWidget {
  const GameSummaryModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Dialog();
  }
}
